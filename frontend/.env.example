# Base URL of the backend API (used once the .NET project is running)
VITE_API_BASE_URL=http://localhost:5200

# While true, services resolve from src/data/mockData.ts instead of the API
VITE_USE_MOCK_DATA=true
